describe("admin spec", () => {
  beforeEach(() => {
    cy.visit("http://localhost:3000/");
  });

  it("admin is able to login", () => {});
  it("admin can see reported squeaks", () => {});
  it("admin can approve a reported squeak", () => {});
  it("admin can deny a reported squeak", () => {});
  it("admin can squeak", () => {});
  it("admin can nut a squeak", () => {});
  it("admin can report a squeak", () => {});
  it("admin can delete their own squeaks", () => {});
  it("admin CANNOT delete another user's squeaks", () => {});
});
